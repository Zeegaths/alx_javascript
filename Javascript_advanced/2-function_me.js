#!/usr/bin/node
// closure using the welcome message
function welcomeMessage(fullName) {
    
    alert("welcome" + " " + fullName)
};
const guillaume = welcomeMessage("guillaume");
const alex = welcomeMessage("Alex");
const fred = welcomeMessage("fred");
